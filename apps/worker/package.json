{
  "name": "@sevo/worker",
  "version": "0.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "rebuild:discovery": "tsx src/modules/discovery/rebuild-public-feed.cli.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@sevo/config": "workspace:*",
    "@sevo/contracts": "workspace:*",
    "@sevo/observability": "workspace:*",
    "@sevo/outbox": "workspace:*",
    "postgres": "3.4.7"
  },
  "devDependencies": {
    "tsx": "4.23.12"
  }
}
