# سوو (Sevo)

سوو یک شبکه فروشگاهی اجتماعی فارسی برای فروشندگان آنلاین فعال ایران است. هر فروشنده فروشگاه مستقل و قابل شخصی‌سازی دارد؛ خریدار می‌تواند از لینک مستقیم وارد شود، کالا ببیند، فروشگاه‌ها را دنبال کند، در فید کشف گردش کند و سفارش خود را درون پلتفرم پیگیری کند.

> نام رسمی محصول «سوو» است و در متن لاتین فقط به صورت `Sevo` نوشته می‌شود. نام مخزن `sevomart` صرفاً شناسه فنی مخزن است.

## وضعیت فعلی

پشته فنی، مرز ماژول‌ها و خط پایه مهندسی نسخه اول پذیرفته شده‌اند. مخزن یک pnpm workspace با web، API، worker، PostgreSQL محلی و کنترل‌های تست و CI دارد؛ قابلیت‌های محصول فقط از Issue ساخت افزوده می‌شوند.

## اجرای محلی کامل (مسیر رسمی)

با Node.js `22.13+`، Corepack و Docker:

```bash
corepack enable
pnpm install
pnpm compose:up
```

این فرمان PostgreSQL، MinIO، migration، API، worker و web را build می‌کند و تا سلامت کامل منتظر
می‌ماند. web در `http://localhost:3100`، API در `http://localhost:3101` و OpenAPI در
`http://localhost:3101/openapi` است. توقف با `pnpm compose:down` داده‌ها را حفظ می‌کند.

برای حلقهٔ سریع hot reload، `pnpm dev` زیرساخت را در Docker و برنامه‌ها را بومی اجرا می‌کند؛ web روی
`3200` و API روی `3201` خواهد بود تا هم‌زمان با Compose یا پروژه‌های رایج دیگر تداخل نداشته باشد. `pnpm quality` کنترل سریع و `pnpm test` کل آزمون‌هاست. جزئیات در
[راهنمای خط پایه توسعه](docs/delivery/development-baseline.md) ثبت شده است.

## از کجا شروع کنیم؟

1. [قواعد همکاری و عامل‌ها](AGENTS.md)
2. [زمینه و زبان مشترک](CONTEXT.md)
3. [تصویر محصول](docs/product/overview.md)
4. [محدوده نسخه اول](docs/product/mvp-scope.md)
5. [نقشه Wayfinder](docs/wayfinder/map.md)
6. [راهنمای کار موازی](docs/delivery/parallel-work.md)
7. [فهرست پژوهش‌ها](docs/research/README.md)
8. [خط پایه توسعه، تست و CI](docs/delivery/development-baseline.md)

## اصل راهنما

دلیل وجود محصول، قابل‌اعتمادتر و قابل‌پیگیری‌تر کردن خرید از فروشندگان آنلاین است. هیچ رابط، متن یا قابلیت نباید سطح حفاظت را بیشتر از واقعیت نشان دهد.

## همکاری

کار از Issue شروع می‌شود، در یک شاخه محدود انجام می‌شود و با Pull Request توسعه‌دهنده دیگر ادغام می‌شود. تصمیم‌های Wayfinder و کارهای ساخت دو backlog جدا هستند؛ جزئیات در [راهنمای مشارکت](CONTRIBUTING.md) آمده است.
