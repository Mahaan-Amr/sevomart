# قواعد همکاری، طراحی و پیاده‌سازی محصول

این فایل برای تمام عامل‌ها و همکارانی است که روی پروژه کار می‌کنند. پیش از پیشنهاد، طراحی یا پیاده‌سازی هر تغییر، `CONTEXT.md`، نقشه فعال در `docs/wayfinder/`، [سیستم طراحی پایه](docs/product/design-system.md) و مشخصات مرتبط را بخوانید. قواعد این فایل برای انسان و عامل یکسان‌اند.

## اصول غیرقابل مذاکره

### ۰. اعتماد و پیگیری پذیری

- دلیل اصلی وجود پلتفرم، قابل اعتمادتر کردن خرید و فراهم کردن مسیر روشن برای پیگیری مشکل است.
- وضعیت سفارش، روش پرداخت، مسئولیت ها و مسیر حل اختلاف باید پیش از تصمیم خریدار روشن باشند.
- هیچ رابط یا متن بازاریابی نباید سطح حفاظتی را بیشتر از واقعیت نشان دهد.
- آمار عمومی فروشگاه به دنبال کنندگان، کالاهای فعال، خریدهای تاییدشده و امتیاز دارای نمونه کافی محدود است.
- بازدید، ذخیره، اشتراک گذاری، افزودن به سبد، رها کردن سبد، نرخ تبدیل و رشد فروش داده خصوصی فروشنده هستند.
- شمار بازدید و پسند کالا عمومی نمی شود و سیگنال محبوبیت قابل دست کاری نباید جای کیفیت و ارتباط را بگیرد.

### ۱. زبان انسانی و دوستانه

- نام مخزن و نام کاری فعلی `SevdoMart` است. نام تجاری عمومی هنوز قطعی نیست؛ در رابط، بازاریابی یا کد دامنه آن را برند نهایی فرض نکنید.
- تمام تجربه کاربر فارسی و راست به چپ است، مگر جایی که ماهیت داده به زبان دیگری نیاز داشته باشد.
- از واژه های آشنا، کوتاه و انسانی استفاده کنید؛ اصطلاح فنی یا اداری را مستقیما به کاربر تحمیل نکنید.
- لحن باید محترمانه، گرم و روشن باشد؛ نه خشک و ماشینی و نه کودکانه.
- نام های تثبیت شده در `CONTEXT.md` مرجع زبان مشترک محصول هستند.
- متن رابط باید به کاربر بگوید چه اتفاقی می افتد و قدم بعدی چیست.

### ۲. مینیمالیسم به عنوان روش کار

- مینیمالیسم فقط سبک ظاهری نیست؛ باید تعداد مراحل، انتخاب ها و بار ذهنی کاربر را کاهش دهد.
- هر صفحه یا فضای کاری دقیقا یک کار اصلی دارد و فقط کارهای فرعی مرتبط با همان کار را نشان می دهد.
- قابلیت های پیشرفته با آشکارسازی تدریجی نمایش داده شوند و مسیر اصلی را شلوغ نکنند.
- پیش فرض های هوشمند، راهنمای درجا و فرم های وابسته به دسته بندی را به تنظیمات و فرم های طولانی ترجیح دهید.
- اطلاعات یا کنترل ضروری را صرفا برای خلوت شدن ظاهر پنهان نکنید. سادگی باید فهم و انجام کار را آسان تر کند.
- افزودن هر قابلیت جدید نیازمند پاسخ روشن به این پرسش است: «کار اصلی کدام صفحه را بهتر می کند؟» اگر پاسخ روشنی ندارد، قابلیت باید جدا، عقب انداخته یا حذف شود.
- مشاهده فروشگاه، فید، کالا و افزودن به سبد نباید به ورود وابسته باشد. ورود فقط هنگام دنبال کردن، گفت وگو، سفارش یا ثبت تجربه خرید درخواست می شود.
- ورود با شماره موبایل و رمز یک بارمصرف مسیر اصلی است؛ ورود گوگل مسیر اختیاری است و شماره معتبر پیش از نخستین اقدام حساس تایید می شود.
- هویت فروشگاه مانند لوگو، رنگ، قلم، تصویر روی جلد و ترتیب بخش های محتوایی قابل شخصی سازی است.
- مسیر خرید و اعتماد، از جمله قیمت، موجودی، سبد، پرداخت، مرجوعی، پیگیری و اختلاف، در همه فروشگاه ها جایگاه و الگوی ثابت دارد.
- شروع شخصی سازی خریدار باید اختیاری، تصویری و کمتر از یک دقیقه باشد؛ چند انتخاب سریع با امکان رد کردن کامل.
- صفحه اصلی دو بخش روشن دارد: «دنبال شده ها» با ترتیب عمدتا زمانی و «کشف» با پیشنهادهای شخصی، تازه و متنوع.
- تبلیغ پولی باید از پیشنهاد طبیعی جدا و با برچسب روشن نمایش داده شود.

### ۳. زبان بصری و حرکت

- [سیستم طراحی پایه](docs/product/design-system.md) برای تمام رابط‌های خریدار و فروشنده الزام‌آور است؛ هر استثنا باید پیش از پیاده‌سازی به‌عنوان تصمیم محصول ثبت شود.
- رنگ اصلی `#A41439` و قلم رابط `Yekan Bakh` از فایل محلی دارای مجوز است. طیف، وزن، اندازه، فاصله و سایه باید از tokenهای همان سند بیاید.
- سبک بصری مینیمال و نئومورفیک کنترل‌شده است. سایه فقط برای عمق یا فهم تعامل استفاده می‌شود؛ card grid، تزئین و برجستگی فراگیر ممنوع است.
- تیتر، متن، کنترل و ظرف بزرگ غیرضروری ساخته نمی‌شود. هر صفحه یک کار اصلی دارد و کارهای مستقل به صفحه یا قدم مستقل شکسته می‌شوند.
- «تمرکز آرام» پیش‌فرض ساخت و ویرایش، «گفت‌وگوی کوتاه» برای راه‌اندازی هدایت‌شده و «پیش‌نمایش زنده» برای تغییر خریدارنما است.
- حرکت باید کوتاه، نرم و کاربردی باشد؛ انیمیشن پیچیده، دائمی یا تزئینی ممنوع است و `prefers-reduced-motion` باید رعایت شود.
- هر تغییر رابط باید در RTL، موبایل، keyboard focus، کنتراست و reduced motion بررسی شود.

## شیوه تصمیم گیری

- ابتدا مسئله و کار اصلی کاربر را تعریف کنید، سپس رابط یا فناوری را انتخاب کنید.
- تصمیم های تاییدشده را بدون مطرح کردن تعارض و گرفتن تصمیم تازه تغییر ندهید.
- نسخه اول را روی کالای فیزیکی و ابزارهای عملیاتی ضروری متمرکز نگه دارید؛ قابلیت های آینده نباید رابط امروز را پیچیده کنند.
- هر پیشنهاد باید هم از دید خریدار و هم از دید فروشنده بررسی شود.

## قواعد قطعی مدل اشتراک

- نشان پلتفرم در همه فروشگاه ها باقی می ماند و حذف آن قابلیت پولی نیست.
- کیفیت یا سرعت پشتیبانی نباید به طرح اشتراک وابسته باشد.
- طرح رایگان باید امکان فروش واقعی را بدهد؛ طرح حرفه ای برای مقیاس، کنترل و رشد کسب وکار است.

## شفافیت پرداخت

- فروشگاه می تواند از تسویه مستقیم یا پرداخت محافظت شده استفاده کند، مشروط به الزامات قانونی و عملیاتی هر روش.
- روش انتخاب شده باید پیش از ثبت سفارش با زبان روشن به خریدار نمایش داده شود.
- تا پیش از بررسی حقوقی، از واژه هایی مانند «ضمانت قطعی» یا «امانت» برای پرداخت محافظت شده استفاده نکنید.
- تسویه مستقیم روش پیش فرض است. پرداخت محافظت شده فقط پس از احراز هویت، تایید حساب بانکی و پذیرش قواعد اختلاف و مرجوعی فعال می شود و در صورت تخلف قابل تعلیق است.
- در پرداخت محافظت شده، مبلغ هنگام اختلاف تا بررسی مدارک و تصمیم پلتفرم آزاد نمی شود.
- در تسویه مستقیم، سیاست مرجوعی فروشنده نمایش داده می شود و پلتفرم شکایت را ثبت و تخلف را پیگیری می کند، اما بازپرداخت را تضمین نمی کند.

## کار با Wayfinder و Issueها

- نقشه Wayfinder محل تصمیم‌های باز است، نه فهرست قابلیت‌هایی که باید فوراً ساخته شوند.
- هر تیکت Wayfinder فقط یک پرسش تصمیم‌ساز دارد و با یکی از برچسب‌های `wayfinder:research`، `wayfinder:prototype`، `wayfinder:grilling` یا `wayfinder:task` مشخص می‌شود.
- پیش از شروع، Issue را به خود اختصاص دهید. Issue باز و بدون مالک تنها کار قابل claim است.
- در متن‌های انسانی، همیشه با عنوان Issue و لینک آن ارجاع دهید؛ شماره تنها کافی نیست.
- پاسخ تصمیم در comment نهایی Issue ثبت، سپس Issue بسته و خلاصه یک‌خطی آن به بخش «تصمیم‌های گرفته‌شده» نقشه افزوده می‌شود.
- کار ساخت فقط از Issue دارای معیار پذیرش و لینک به spec/decision شروع می‌شود. ابهام محصول را با حدس داخل کد حل نکنید؛ Issue تصمیم بسازید یا آن را در نقشه ثبت کنید.

## همکاری موازی دو توسعه‌دهنده

- هر شاخه فقط یک Issue اصلی دارد و با الگوی `feat/<issue>-<slug>`، `fix/<issue>-<slug>` یا `docs/<issue>-<slug>` نام‌گذاری می‌شود.
- مسیر خریدار و مسیر فروشنده می‌توانند موازی باشند، اما قراردادهای مشترک—هویت، فروشگاه، کالا، سفارش، پرداخت، گفت‌وگو و اختلاف—پیش از پیاده‌سازی مستقل مستند می‌شوند.
- تغییر در قرارداد یا فایل مشترک باید پیش از کدنویسی در Issue اعلام شود. دو شاخه نباید هم‌زمان همان قرارداد یا migration را تغییر دهند.
- مالک Issue فقط صاحب هماهنگی است، نه مالک دائمی یک بخش. تصمیمی که مصرف‌کننده دیگری دارد باید با او مرور شود.
- Pull Request مسیر پیش‌فرض است و باید کوچک، قابل بازبینی و دارای Issue، محدوده، معیار پذیرش، تست و اثر بر قراردادها باشد.
- push مستقیم روی `main` فقط با درخواست صریح مالک مخزن در همان کار مجاز است؛ شاخه باید نسبت به `origin/main` به‌روز و fast-forward باشد، محدوده تغییر تأیید شده باشد و بررسی‌های مرتبط پیش از push سبز باشند. در نبود این اجازه صریح، مرور توسعه‌دهنده دیگر و Pull Request الزامی است.
- تغییرات خارج از محدوده Issue، بازآرایی گسترده و dependency جدید در همان PR انجام نمی‌شوند مگر اینکه صریحاً برای معیار پذیرش لازم باشند.

## قواعد مهندسی

- تا زمان بسته‌شدن تصمیم معماری و پشته فنی، چارچوب یا سرویس خارجی را به‌عنوان پیش‌فرض قطعی وارد نکنید.
- مرزهای دامنه را بر اساس زبان `CONTEXT.md` حفظ کنید؛ واژه‌های متفاوت برای یک مفهوم واحد نسازید.
- منطق اعتماد، پرداخت، سفارش و اختلاف باید قابل آزمون، قابل ممیزی و مستقل از متن رابط باشد.
- داده حساس، رمز، token، شماره موبایل و اطلاعات بانکی در مخزن، fixture واقعی، log یا Issue ثبت نمی‌شود.
- هر رفتار مهم حداقل یک آزمون خودکار در سطح مناسب دارد. bug ابتدا با آزمون بازتولید می‌شود.
- رابط خریدار و فروشنده فارسی و RTL است؛ layout، validation، اعداد، تاریخ، پول و متن‌های طولانی فارسی باید آزموده شوند.
- وابستگی جدید فقط با دلیل، وضعیت نگهداری، مجوز و اثر امنیتی ثبت‌شده پذیرفته می‌شود.
- مستندات هم‌زمان با تغییر قرارداد یا رفتار به‌روزرسانی می‌شوند؛ کد تنها منبع حقیقت تصمیم محصول نیست.
