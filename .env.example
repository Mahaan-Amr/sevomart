# Local-only defaults. Copy to .env only when an override is needed.
NODE_ENV=development
API_PORT=3001
WEB_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://sevo:sevo_local@localhost:6432/sevo
OTEL_EXPORTER_OTLP_ENDPOINT=
