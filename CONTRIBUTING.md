# راهنمای مشارکت

## پیش از شروع

- `AGENTS.md` و `CONTEXT.md` را کامل بخوانید.
- Issue آزاد را claim و به خود assign کنید.
- وابستگی‌ها و spec پیوندخورده را بررسی کنید.
- اگر معیار پذیرش یا قرارداد مشترک روشن نیست، کدنویسی را شروع نکنید.

## شاخه و Pull Request

- `feat/<issue>-<slug>` برای قابلیت
- `fix/<issue>-<slug>` برای اصلاح
- `docs/<issue>-<slug>` برای مستندات
- `spike/<issue>-<slug>` برای نمونه فنی دورریختنی

هر PR باید این موارد را داشته باشد:

- لینک Issue و توضیح مسئله
- محدوده تغییر و موارد خارج از محدوده
- روش راستی‌آزمایی و تست‌ها
- اثر بر قرارداد، migration، امنیت و RTL
- تصویر یا ویدیو برای تغییر قابل مشاهده

## تعریف انجام‌شدن

- معیارهای پذیرش Issue برآورده شده‌اند.
- تست‌های مرتبط اضافه و سبز هستند.
- رفتار فارسی/RTL بررسی شده است.
- مستندات و قراردادهای مرتبط به‌روز شده‌اند.
- داده حساس یا secret وارد مخزن نشده است.
- توسعه‌دهنده دیگر PR را مرور کرده است.

## تصمیم در برابر ساخت

Issueهای `wayfinder:*` برای رفع ابهام و ثبت تصمیم‌اند. Issueهای `kind:build` برای پیاده‌سازی تصمیم‌های بسته‌شده‌اند. یک PR ساخت نباید تصمیم محصول تازه‌ای را پنهانی تثبیت کند.
